NexMind Dashboard (Vite + React + Tailwind)

Quick start:

1. cd frontend-react
2. npm install
3. npm run dev

This is a responsive, mobile-first dashboard scaffold designed for production use. Customize components in `src/components` and Tailwind configuration in `tailwind.config.cjs`.
